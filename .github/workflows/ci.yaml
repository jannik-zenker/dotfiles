# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/ci.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  build:
    env:
      NIX_CONFIG: |-
        extra-substituters = https://cache.jannikzenker.de/dotfiles
        extra-trusted-public-keys = dotfiles:2EaWL5tiEyYzC5KdZeR6f/V9DbVQZUQthxiVg19I3nk=
    name: Build ${{ matrix.host.hostname }}
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v6
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@main
    - name: Build
      run: |-
        nix build \
          ".#${{ matrix.host.output }}" \
          --out-link result \
          --print-build-logs
    - name: Install Attic
      run: nix profile install nixpkgs#attic-client
    - env:
        ATTIC_TOKEN: ${{ secrets.ATTIC_TOKEN }}
      if: |-
        github.event_name != 'pull_request' ||
        github.event.pull_request.head.repo.full_name == github.repository
      name: Login to Attic
      run: |-
        attic login \
          --set-default \
          ci \
          https://cache.jannikzenker.de \
          "$ATTIC_TOKEN"
    - if: |-
        github.event_name != 'pull_request' ||
        github.event.pull_request.head.repo.full_name == github.repository
      name: Push to Attic
      run: attic push dotfiles result
    strategy:
      fail-fast: false
      matrix:
        host:
        - hostname: hauler
          output: nixosConfigurations.hauler.config.system.build.toplevel
        - hostname: lumiere
          output: nixosConfigurations.lumiere.config.system.build.toplevel
        - hostname: reacher
          output: nixosConfigurations.reacher.config.system.build.toplevel
        - hostname: sirene
          output: nixosConfigurations.sirene.config.system.build.toplevel
    timeout-minutes: 60
  flake-check:
    env:
      NIX_CONFIG: |-
        extra-substituters = https://cache.jannikzenker.de/dotfiles
        extra-trusted-public-keys = dotfiles:2EaWL5tiEyYzC5KdZeR6f/V9DbVQZUQthxiVg19I3nk=
    name: Flake check
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v6
    - name: Install Nix
      uses: DeterminateSystems/nix-installer-action@main
    - name: Check flake
      run: nix flake check
    timeout-minutes: 60
name: CI
'on':
  pull_request: {}
  push:
    branches:
    - main
  workflow_dispatch: {}
permissions:
  contents: read
